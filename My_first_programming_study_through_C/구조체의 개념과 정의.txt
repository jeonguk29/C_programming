구조체는 여러 개의 데이터를 하나로 묶어서 사용할 수 있도록 하기 위해 만들어진 
c언어의 문법입니다. 후에 이러한 구조체의 개념은 객체 지향 프로그래밍에서는 클래스라는 
개념으로 확장되어 사용합니다. 배열이 여러 개의 같은 자료형들을 하나로 묶는 것이 였다면 
구조체는 서로 다른 자료형들을 하나로 묶어서 한꺼번에 다루는 것입니다.

구조체 : 설계의 측면에서 더의미가 있음
예를들어 struct number{  스트럭트 넘버라는 이름에 구조체를 만든다면 이안에 들어가는 
         int img;        내용으로 인트 이미지 
         floatreal;      플랏리얼 이런것들이 들어갈수 있는것임 
         };


만약에 인공위성 관련 프로그램을 만든다고 하면

이렇게 지도가 있다고 했을때  

A가 집이라고 했을때 B라는 지점으로 도달할수 있는 가장 최적에 경로를 구한다던지 다양한
프로그래밍 기법들을 구현한다고 가정했을때
ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ      
            | A |                                
ㅡㅡㅡㅡㅡㅡ     ㅡㅡㅡㅡㅡㅡ    ㅡㅡㅡㅡㅡ     
            |   |            |   |    
            |   |            |   |
            |   |            |   |
            |   |____________|   |
            | C                B |
            ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ

이때 각각 에 A,B,C 이런 점들은 어떤식으로 데이터들을 관리할수 있을까요
바로 지점 이라는 틀이 있다고 했을때

이틀안에 들어가는 속성으로는                     

지점{
      x좌표
      y좌표
      해당지역이름
      자세한 주소 
          .                                           
          .                                           
          . 이러한 다양한 내용들이 다양한 속성들이 이지점이라는 틀안에 들어갈수 있을것임                
이게 바로 구조체가 존재하는 이유라고 할수 있음 

이거 같은 경우는 그대로 소스 파일로 구현한다면 다음과 같이 구현할수있음

steuct 지점
{
int x,y
char[]=이름
}                 이런식으로 바로 특정한 실질적으로 코딩할때 사용되는것중에서
                  가장많이 사용되는게 바로 이 구조체라는것인데요 어떠한 틀이 있을때
                  그안에 들어갈수 있는 내부적인 변수 같은걸 한꺼번에 이렇게 지정해 
                  주므로써 이렇게 이 steuct 지점 이라는 변수를 만들어 주게 되면
                  int x,y
                  char[]=이름
                  }              이러한 변수들이 한꺼번에 딸려서 묶어서 사용이 될수 있도록
                   해준다는것임 만약에 이러한 구조체가 없다면 코딩은 가능하겠지만 정말 보는                   데 불편하고 설계적인 측면에서 굉장히 큰단점에 눈에 보일것임 


그래서 이러한것들을 해결하기 위해서
나온것이 바로이 구조체라고 할수 있음
즉 저저번 시간에 배웠던 배열은 여러 개의 같은 자료형들을 하나로 묶는 것이 였다면 
구조체는 서로 다른 자료형들을 하나로 묶어서 한꺼번에 다루는 것을 의미한다고 할수 있음.
                           
       
구조체는 밑에처럼 내부적으로 메모리에서 이런식으로 작동 한다고 할수 있음

struct song  송이라는 이름에 구조체를 만들었다고 하면 그 내부적인 속성으로 
내임과 랭스 라는것을 보관할수 있는데 
NAME            Length                   네임같은 것은 스트링 즉 문자열 케릭터 배열 형태로  
ㅡㅡㅡㅡㅡㅡㅡ  ㅡㅡㅡㅡㅡ               만들것이고 
             |  |          |             랭스같은것은 아무래도 더블형이나 플랏형같은 
ㅡㅡㅡㅡㅡㅡㅡ  ㅡㅡㅡㅡㅡ               실수 형으로 만들것임 
                            그렇게 만든 다음에   

title 1 라는 이름에 위에 송 구조체에 변수를 만들고 그안에 들어가는 
  NAME            Length                  
ㅡㅡㅡㅡㅡㅡㅡ  ㅡㅡㅡㅡㅡ           
 ooowee babe |  | 2.40   |     네임이라는 변수에는 이렇게 이름을 넣고  
ㅡㅡㅡㅡㅡㅡㅡ  ㅡㅡㅡㅡㅡ     길이같은 경우는 2.40 분이다 이런식으로 한번 넣을수 있는것임    













 
